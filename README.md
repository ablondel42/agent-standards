# agent-standards

Reusable production-grade agent instruction files for software projects.

## Structure
- `setup/`: editor-specific installation guidance
- `shared/`: cross-cutting rules shared across roles
- `roles/`: role-specific rule sets
- `templates/`: starter templates for future roles and mappings
- `meta/`: maintenance and taxonomy docs
